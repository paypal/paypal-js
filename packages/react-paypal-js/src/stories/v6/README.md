# V6 Stories

Storybook stories for the V6 PayPal SDK components.

## Setup

Start the sample integration server at `http://localhost:8080` (or set `STORYBOOK_PAYPAL_API_URL`).

## Structure

```
v6/
├── decorators/          # withPayPalProvider decorator
└── buttons/             # Button component stories
```
